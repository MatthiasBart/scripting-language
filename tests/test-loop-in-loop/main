{ a b |
   b = 9

   lp@{|
        a = 3
        loopi@{c |
            a ->

            c = b < 7
            c ? {|lp^}

            a = a + -1
            a ? {|} : {|loopi^}
        }

        b ->
        b = b + -1
   }
}
